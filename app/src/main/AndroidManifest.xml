<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="my.edu.tarc.hciprototype">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".PurchaseTicketActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SelectTaskActivity" />
        <activity android:name=".MakePayment" />
        <!-- <activity android:name=".SelectBusActivity"/> -->
        <activity android:name=".SelectSeat" />
        <!-- <activity android:name=".MakePayment"></activity> -->
        <activity android:name=".BankDetailActivity" />
        <activity android:name=".BankOTPActivity" />
        <activity android:name=".PaymentMadeActivity" />
        <activity android:name=".ViewModifyTicket" />
        <activity android:name=".ViewTicket" />
        <activity android:name=".EditTicket" />
        <activity android:name=".EditSeat" />
        <activity android:name=".ModifiedTicketActivity" />
        <activity android:name=".EditTicketSelectedBusActivity"></activity>
    </application>

</manifest>